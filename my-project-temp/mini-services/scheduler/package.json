{
  "name": "apical-scheduler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts"
  },
  "_note": "@prisma/client is intentionally not a dependency here: bun resolves it up-tree to the single root-generated client, so this service can never drift from the root Prisma schema.",
  "dependencies": {}
}
